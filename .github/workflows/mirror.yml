name: Mirror to Personal Repository

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config --global user.name "${{ secrets.MIRROR_USER_NAME }}"
          git config --global user.email "${{ secrets.MIRROR_USER_EMAIL }}"

      - name: Push to mirror repository
        run: |
          git remote add mirror https://${{ secrets.MIRROR_USER_NAME }}:${{ secrets.MIRROR_PAT }}@github.com/${{ secrets.MIRROR_REPO }}.git
          git push mirror --all --force
          git push mirror --tags --force
